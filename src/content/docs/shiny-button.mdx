---
title: Shiny Button
description: An animated marquee component that smoothly scrolls content horizontally, adding dynamic motion to your website with Framer Motion.
date: 2024-11-05
---

<ComponentPreview name="shiny-button-demo" description="Shiny Button" />

## Installation

```bash
npm i clsx tailwind-merge framer-motion
```

<Steps>

<Step>Add util file</Step>

```ts title="lib/utils.ts"
import { clsx, ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the code into your project.</Step>

components/ShinyButtonDemo.tsx

<ComponentSource name="shiny-button" />

<Step>Update the imports paths to match your project setup.</Step>

<Step>Add the code in your css file</Step>

```css title="global.css"
.radial-gradient {
  background: radial-gradient(
      circle at 50% 0%,
      rgba(250, 250, 250, 0.05) 0%,
      transparent 60%
    ), rgba(15, 15, 15, 1);
}

.linear-mask {
  mask-image: linear-gradient(
    -75deg,
    white calc(var(--x) + 20%),
    transparent calc(var(--x) + 30%),
    white 60%
  );
  -webkit-mask-image: linear-gradient(
    -75deg,
    white calc(var(--x) + 15%),
    transparent calc(var(--x) + 25%),
    white 60%
  );
}
.linear-overlay {
  background-image: linear-gradient(
    -75deg,
    rgba(255, 255, 255, 0.1) calc(var(--x) + 20%),
    rgba(255, 255, 255, 0.5) calc(var(--x) + 25%),
    rgba(255, 255, 255, 0.1) calc(var(--x) + 100%)
  );
  mask: linear-gradient(black, black) content-box, linear-gradient(black, black);
  -webkit-mask: linear-gradient(black, black) content-box, linear-gradient(
      black,
      black
    );
  mask-composite: exclude;
}
```

</Steps>
