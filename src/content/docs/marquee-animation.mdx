---
title: Marquee Animation
description: An animated marquee component that smoothly scrolls content horizontally, adding dynamic motion to your website with Motion.
date: 2024-11-05
---

<ComponentPreview  name="MarqueeAnimationDemo"  description="Marquee animation"/>

## Installation

```bash
npm i clsx tailwind-merge motion
```

<Steps>

<Step>Add util file</Step>

```ts title="lib/utils.ts"
import { clsx, ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the code into your project.</Step>

components/Marquee.tsx

<ComponentSource name="MarqueeAnimation" />

<Step>Update the imports paths to match your project setup.</Step>

</Steps>
